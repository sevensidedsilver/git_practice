// this function takes 3 lengths as arguments and simply determines if those sides could form a triangle

function isTriangle(a,b,c) {
   if ( (a+b > c && b+c > a && a+c > b) ) {
   return true;
   } else {
   
   return false;
}}